name: Page List
settings:
  pages:
  type: Page
  multiple: true
template: >
  <ul>
    {% for page in item.pages %}
      <li><a href="{{page | permalink}}">{{page.title}}</a></li>
    {% endfor %}
  </ul>
