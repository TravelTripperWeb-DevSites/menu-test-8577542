name: Page List
settings:
  pages:
    type: page
    multiple: true
template: >
  <ul>
    {% for page in item.pages %}
      <li><a href="{{page | permalink}}">{{page.title_localized.en}} {{site.active_lang}}</a></li>
    {% endfor %}
  </ul>
