name: Page List
settings:
  pages:
    type: page
    multiple: true
template: >
  <ul>
    {% for page_id in item.pages %}
      {% assign page = site.pages[page_id] %}
      <li><a href="{{page | permalink}}">{{page}} {{site.active_lang}}</a></li>
    {% endfor %}
  </ul>
